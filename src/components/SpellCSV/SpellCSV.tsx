import React from 'react';
import style from './SpellCSV.module.scss';
type AlertProps = {
};
const SpellCSV = ({}: AlertProps): JSX.Element => {
    return (
        <div className={style.Main}>
            SpellCSV
        </div>
    );
}

export default SpellCSV;